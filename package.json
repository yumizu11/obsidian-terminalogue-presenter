{
  "name": "obsidian-terminalogue-presenter",
  "version": "0.5.2",
  "description": "Obsidian plugin that turns a Marp note into an HTML presentation with animated Terminalogue terminals, using your own Marp CLI installation.",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "watch": "node ./scripts/build.mjs --watch",
    "clean": "node -e \"fs.rmSync('main.js',{force:true});fs.rmSync('main.js.map',{force:true});fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('src/generated',{recursive:true,force:true})\"",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node ./scripts/build.mjs && node --test test/command-line.test.mjs test/executable.test.mjs test/runner.test.mjs test/process-manager.test.mjs test/session.test.mjs test/commands.test.mjs test/bundle.test.mjs",
    "check": "npm run build && npm run lint && npm run typecheck && npm run test",
    "deploy": "node ./scripts/deploy.mjs",
    "release": "node ./scripts/release.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.9.1",
    "eslint-plugin-obsidianmd": "^0.4.2",
    "globals": "^17.12.0",
    "obsidian": "^1.13.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.69.0"
  }
}
